{
  "schema": 1,
  "researched": "2026-08-03",
  "sources": [
    {
      "id": "src-plan9-overview",
      "url": "https://css.csail.mit.edu/6.824/2014/papers/plan9.pdf",
      "title": "Plan 9 from Bell Labs",
      "kind": "paper",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-9p-io-9",
      "url": "https://9p.io/sys/doc/9.html",
      "title": "Plan 9 from Bell Labs (canonical overview essay)",
      "kind": "official-docs",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-9p-io-names",
      "url": "https://9p.io/sys/doc/names.html",
      "title": "The Use of Name Spaces in Plan 9",
      "kind": "official-docs",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-namespace-man",
      "url": "https://man2.aiju.de/4/namespace",
      "title": "namespace(4) — the Plan 9 manual",
      "kind": "man-page",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-bind-man",
      "url": "http://man.cat-v.org/plan_9/1/bind",
      "title": "bind(1) — the Plan 9 manual",
      "kind": "man-page",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-net-doc",
      "url": "https://9p.io/sys/doc/net/net.html",
      "title": "The Organization of Networks in Plan 9",
      "kind": "official-docs",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-namespace-dummies",
      "url": "https://blog.darknedgy.net/technology/2014/07/31/0-9ns/",
      "title": "The Plan 9 Namespace for Dummies",
      "kind": "blog-explainer",
      "accessed": "2026-08-03"
    },
    {
      "id": "src-wikipedia-9p",
      "url": "https://en.wikipedia.org/wiki/9P_(protocol)",
      "title": "9P (protocol)",
      "kind": "reference",
      "accessed": "2026-08-03"
    }
  ],
  "structure": {
    "rationale": "The book follows the split the requester specified: Part 1 (The Plan 9 Model) lays down the three composing pillars — files as the uniform resource interface, 9P as the single protocol that reaches them, and the per-process namespace that assembles them into a program's view — using the 'Plan 9 from Bell Labs' overview paper as the grounding source, with no history or biography. Part 2 then walks outward from the same three pillars in dependency order: Namespaces (the abstraction itself) before 9P Protocol (the wire mechanism namespaces are built from) before File Servers (the things namespaces name and 9P talks to); Mount, Bind, and Union Directories follow as the operators that construct a namespace from those servers; Process Namespaces then shows how that construction interacts with rfork/process lifecycle; Plumbing is placed next as a worked example of the whole model — an ordinary file server used as an interprocess bus; and Security and the Capability Model closes the book, since Plan 9's security design (no setuid, factotum, capabilities) is best understood as a consequence of everything established before it — namespace restriction standing in for privilege. Each concept builds strictly on the ones before it, so a reader who proceeds in order never meets an unexplained mechanism.",
    "audience": "Technically savvy readers already comfortable with systems programming and Unix-family OS concepts, who want Plan 9's actual design and mechanisms — not its history — explained precisely."
  },
  "concepts": {
    "the-plan-9-model": {
      "researched": "2026-08-03",
      "claims": [
        {
          "text": "Plan 9's design rests on three composing ideas: resources are named and accessed like files in a hierarchical file system; a single protocol, 9P, is used to access those resources whether they are local or remote; and the disjoint hierarchies exposed by different services are joined by the client into one private hierarchical name space.",
          "sources": ["src-plan9-overview", "src-9p-io-9"]
        },
        {
          "text": "Plan 9 applies the file/directory metaphor far more broadly than traditional Unix: windows, network connections, running processes, the mouse, the graphics device, and remote machines' resources are all represented as files or directory trees that are opened, read, written, and closed with the same small set of operations, rather than through separate special-case system calls (ioctl, socket options, and the like).",
          "sources": ["src-plan9-overview", "src-9p-io-9", "src-net-doc"]
        },
        {
          "text": "9P is the one protocol used to reach every resource in the system: the kernel speaks it to serve local devices, and the identical protocol is used over a network connection to talk to a file server on another machine, so a remote resource is reached exactly the same way as a local one.",
          "sources": ["src-plan9-overview", "src-wikipedia-9p"]
        },
        {
          "text": "Every process has its own private name space — its own view of the file hierarchy — rather than sharing one global file tree; a name space is assembled by binding and mounting resource trees (served locally by the kernel or remotely by a 9P server) at chosen points.",
          "sources": ["src-9p-io-names", "src-namespace-man"]
        },
        {
          "text": "Because a name space is just a client-side composition of file trees reached over 9P, building a custom view of the system requires no new system calls or kernel support for each new kind of resource — adding a resource to your world is a bind or mount, not a new API.",
          "sources": ["src-plan9-overview", "src-9p-io-names"]
        },
        {
          "text": "The window system, 8½, is itself an ordinary file server: it serves files such as /dev/cons and /dev/bitblt, and gives each window client a distinct copy of those files in that window's own local name space, so the same path name (e.g. /dev/cons) resolves to a different underlying resource depending on whose name space is doing the resolving.",
          "sources": ["src-9p-io-names"]
        },
        {
          "text": "Conventional path names (such as /net, /dev, /proc, /bin) are consistent across every Plan 9 machine, but what is actually bound or mounted underneath those names — and therefore what a program actually finds there — can differ per process and per machine; a program always knows where to look, not necessarily what it will find.",
          "sources": ["src-namespace-dummies", "src-namespace-man"]
        },
        {
          "text": "Some conventional directories are union directories — a single directory backed by several component directories bound together and searched in order — used, for example, so that /bin resolves to the right set of executables for the current architecture without a program needing to know which one is active.",
          "sources": ["src-namespace-man", "src-bind-man"]
        },
        {
          "text": "Network access is exposed as a file hierarchy under /net: a protocol directory (e.g. /net/tcp) contains a clone file used to reserve a new connection (reading it returns a connection number), and each numbered connection directory exposes ctl and data files — writing a connect command to ctl establishes the connection, and data carries the bytes — so dialing out is ordinary file I/O rather than a socket system call.",
          "sources": ["src-net-doc"]
        }
      ],
      "snippets": [
        {
          "lang": "text",
          "code": "open  /net/tcp/clone            -> read: connection number \"n\"\nwrite /net/tcp/n/ctl  \"connect 192.0.2.1!80\"\nwrite /net/tcp/n/data  ...      -> bytes sent on the connection\nread  /net/tcp/n/data           -> bytes received on the connection",
          "note": "The classic /net dialing sequence, simplified from the protocol-directory description in net.html — plain opens/reads/writes on ctl and data files, no socket(2)/connect(2) call involved.",
          "sources": ["src-net-doc"]
        },
        {
          "lang": "text",
          "code": "; ns\nbind -a /386/bin /bin\nbind -a /rc/bin /bin\nbind '#c' /dev\nbind '#d' /fd\nmount -A /srv/boot /n/other",
          "note": "Illustrative shape of `ns` output (per-process namespace listing) — bind/mount lines built from kernel devices ('#c', '#d') and mounted 9P services; exact lines vary by machine and session.",
          "sources": ["src-namespace-dummies", "src-namespace-man"]
        }
      ],
      "pitfalls": [
        "Readers from a Unix background tend to assume a bind or mount is a global, persistent change (like editing /etc or adding a symlink). In Plan 9 it changes only the acting process's name space (and any process that shares it) — the same bind performed in one window is invisible from another window's shell unless the name space is explicitly shared or the change is made before the fork that separated them.",
        "\"Everything is a file\" in Plan 9 is easy to under-read as just Unix's version of the same slogan. Plan 9 pushes further: Unix still special-cases sockets, ioctls, and many device controls outside the ordinary file interface, whereas Plan 9 routes control operations themselves (e.g. dialing a network connection, above) through plain opens/reads/writes on ctl and data files, which is what lets one protocol (9P) and one client interface cover them all.",
        "The three pillars are easy to state as a checklist and easy to under-appreciate as a system: the payoff is specifically that they compose — file-naming (pillar 1) is what makes one protocol sufficient (pillar 2), and one protocol is what makes client-side name-space assembly (pillar 3) possible without kernel changes for every new resource type. Explaining any one pillar in isolation, without the composition, misses the actual design contribution."
      ],
      "furtherReading": ["src-plan9-overview", "src-9p-io-9", "src-9p-io-names"]
    },
    "namespaces": {},
    "9p-protocol": {},
    "file-servers": {},
    "mount-bind-union": {},
    "process-namespaces": {},
    "plumbing": {},
    "security-model": {}
  },
  "volatile": [
    "9P2000.u / 9P2000.L dialect details across Plan 9 vs 9front vs Linux v9fs",
    "specific man-page section numbers and file paths, which differ slightly between distributions (Plan 9 4th edition vs 9front)"
  ]
}
