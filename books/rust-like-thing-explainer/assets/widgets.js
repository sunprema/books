/* widgets.js — this book's own canvas-widget definitions.
   Boots on the shared runtime vendored at assets/vendor/book-widgets.js
   (load that script first, then this one — see widgets/README.md).

   Empty scaffold: concept pages register widgets here as they are built,
   with BookWidgets.register('name', function(box, W){ ... }). See the
   runtime README for the API (Plot, drag, anim, onRelayout, theme, rng)
   and the markup contract (<figure class="figbox" data-widget="name">). */
