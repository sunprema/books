/* Protobuf — book-local widget definitions, registered on the shared
   assets/vendor/book-widgets.js runtime (BookWidgets.register(name, init)).
   Scaffold stub: no widgets registered yet. Candidates as concept pages are
   written — a varint/tag byte-encoder for "how-serialization-works", a
   wire-type inspector for "performance-and-tradeoffs" — get added here, one
   BookWidgets.register(...) call per widget, following widgets/README.md. */
