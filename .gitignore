# Narrowed from `.claude/` so .claude/settings.json can be committed — the
# bookbank cloud routine reads it from this checkout to load the BookBank
# plugin at session start. Only the personal/local override stays ignored.
.claude/settings.local.json
